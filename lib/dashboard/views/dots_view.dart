import 'package:flutter/material.dart';

class DotsView extends StatelessWidget {
  const DotsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dots'),
    );
  }
}
